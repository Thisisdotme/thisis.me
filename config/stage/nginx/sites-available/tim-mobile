server {
  listen              80;
  access_log          on;
  error_log           /var/log/nginx/tim-mobile.error.log;

  charset             utf-8;
  location / {
      uwsgi_pass      unix:///tmp/tim-mobile.sock;
      include         uwsgi_params;
      uwsgi_param     SCRIPT_NAME /;
  }
}
