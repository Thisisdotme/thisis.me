{#content}
 <div class="content follow">
		{?photo_url}
		  <div class="inner-image">
				<img src='{photo_url}' />
			</div>
		{:else}
		{/photo_url}
		   <div class="inner-text">
			    {?label}
			     <p>{label}</p>
			    {/label}
			    {?data}
			     <p>{data}</p>
			    {/label}
		   </div>
	</div>
{/content}
{>_eventFooter/}

