 <div class="content checkin">
		{?photo}
		   	<div class="inner-image">
				<img src='{photo.image_url}' />
			</div>
		{:else}
		{/photo}
     <div class="inner-text">
  	    {?headline}
  	     <p>{headline}</p>
  	    {/headline}
  	    {?content}
  	     <p>{content}</p>
  	    {/content}
     </div>
  </div>

{>_eventFooter/}

